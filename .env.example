# === LLM ===
# Lấy API key tại: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# === Redis (tuỳ chọn — app chạy bình thường không có Redis) ===
# REDIS_URL=redis://localhost:6379/0

# === Paths (tuỳ chọn — có default tự động) ===
# VECTORDB_PATH=backend/data/vectordb
# TEMPLATES_DIR=backend/templates
# OUTPUTS_DIR=backend/outputs

# === URLs ===
# PUBLIC_API_URL=http://localhost:8000
# GENERATE_CV_URL=http://localhost:8000

# === OnlyOffice (tuỳ chọn) ===
# ONLYOFFICE_DOCUMENT_SERVER_URL=http://localhost:8080
