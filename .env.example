# Sao chép file này thành .env và điền giá trị thực
# cp .env.example .env

# ── Bắt buộc ──────────────────────────────────────────────────────────────────
# Lấy tại https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your_openai_api_key_here

# ── Java backend (tùy chọn — chỉ cần nếu chạy tính năng xác thực/hồ sơ) ──────
DB_USERNAME=cvcraft
DB_PASSWORD=cvcraft123
PGUSER=cvcraft
PGPASSWORD=cvcraft123
PGDATABASE=cvcraft_db
